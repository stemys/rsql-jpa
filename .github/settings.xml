<settings>
    <servers>
        <server>
            <id>nexus-stemys</id>
            <username>{{ secrets.NEXUS_USERNAME }}</username>
            <password>{{ secrets.NEXUS_PASSWORD }}</password>
        </server>
    </servers>
</settings>